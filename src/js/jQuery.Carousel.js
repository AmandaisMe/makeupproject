;(function($){
	$.fn.MyCarousel=function(option){
		//给默认值
		var defaults={
			width:1920,
			height:350,
			times:3000,
			position:"right",
			page:true

		}
		var opt=$(this).extend({},defaults,option);
		//初始化
		function init(){
			//生成html结构
			
		}

	}


})(jQuery);